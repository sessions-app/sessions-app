import PropTypes from 'prop-types';

const track = {
  name: PropTypes.string,
  id: PropTypes.string,
  artist: PropTypes.string,
};

export {
  track, // eslint-disable-line
  /* Remove eslint line when more shapes are added */
};
